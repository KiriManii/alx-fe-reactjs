/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{js,jsx,ts,tsx}', './public/index.html'], // Files Tailwind scans for classes
  theme: {
    extend: {}, // Use this section for customizations if needed later
  },
  plugins: [], // Add Tailwind plugins here if required
};
